
{{#each blogPosts as |post| }}
<div class="">
    <div class="">
        <h2>{{title}}</h2>
    </div>
    <div class="">
        <h4>Category: {{pet_category}}</h4>
        <h4>Type: {{post_type}}</h4>
        {{!-- not sure how to get user name from userID yet --}}
        <h3>User: {{user_id}}</h3>
        <p>{{body}}</p>
    </div>

</div>
{{/each}}

<script src="./js/blogPost-all.js"></script>